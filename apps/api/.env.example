# Única variável no .env — gerada automaticamente na primeira execução.
# Demais configurações (DATABASE_URL, CLAUDE_COMMAND, TIMEOUT, EVOLUTION_*)
# são definidas pelo wizard de setup dentro do aplicativo (app-config.json).
ENCRYPTION_KEY=your_encryption_key_here
